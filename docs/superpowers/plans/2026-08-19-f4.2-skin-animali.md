# F4.2 — Skin/colori per animali e piante · Piano di implementazione

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** La coppia compra 8 colori globali (50 monete l'uno) riusabili su qualunque animale/pianta posseduto, li attiva/disattiva gratis in ogni momento su un animale alla volta — il colore si vede come un filtro CSS applicato all'emoji/immagine della specie.

**Architecture:** Zero nuova infrastruttura di acquisto: `item_prices`/`owned_items`/`purchase_item` (F6) restano invariati, ricevono solo 8 nuove righe. Una colonna sola (`pets.active_skin`) e una funzione sola (`select_pet_skin`) bastano per l'attivazione. Il filtro colore vive come CSS puro, applicato in `SpeciesImage` sia sull'`<img>` sia sul fallback emoji.

**Tech Stack:** Stesso di F0+F1/F5/F6/F2/F3/F4.1 — Next.js App Router, TypeScript strict, `@supabase/supabase-js`, Vitest (unit in jsdom, integrazione contro Supabase locale).

**Spec di riferimento:** [`docs/superpowers/specs/2026-08-19-f4.2-skin-animali-design.md`](../specs/2026-08-19-f4.2-skin-animali-design.md)

**Branch:** nuovo branch da `main` (che contiene già F0+F1, F5, F6, F2, F3.1, F3.2, F4.1 in produzione), in un worktree isolato

## Global Constraints

- Lingua dell'interfaccia: inglese. Commenti e documentazione in italiano.
- Mobile-first, viewport di riferimento 390 × 844 px. Ogni target di tocco almeno 44 × 44 px.
- Nessuna scrittura diretta dal client: solo `.select()` e `.rpc()`.
- Nessun colore letterale nei componenti di layout: solo variabili CSS già definite in `app/globals.css`. Eccezione deliberata: i filtri CSS e gli hex dei swatch delle skin in `features/pets/skins.ts` sono per natura colori decorativi arbitrari, non token semantici dell'interfaccia — stesso principio già in vigore per `features/shop/themes.ts`.
- TypeScript strict: nessun `any` implicito, nessun `@ts-ignore`.
- Ogni tabella/funzione/colonna nuova nasce con zero privilegi di default (convenzione di progetto): ogni migrazione include `grant`/`revoke` espliciti dove serve.
- `item_prices`, `owned_items`, `purchase_item` (F6) restano invariati — solo nuove righe in `item_prices`, nessuna nuova migrazione di schema per loro.
- Il costo di ogni skin vive SOLO in `item_prices`, mai duplicato in `skins.ts` — stesso principio già in vigore per `themes.ts` (F6) e `species.ts` (F4.1).
- Attivare una skin già posseduta è sempre gratuito e immediato, in ogni momento — mai un secondo acquisto per riattivare un colore già comprato.
- Dopo ogni migrazione, rigenerare `lib/types.ts` con `npm run db:types` e includerlo nel commit.
- Commit dopo ogni task, messaggio in italiano, prefisso convenzionale (`feat:`, `test:`, `fix:`).

## Mappa dei file

| File | Responsabilità |
|---|---|
| `supabase/migrations/20260824090000_pets_active_skin.sql` | `alter table pets add column active_skin text null` |
| `supabase/migrations/20260824091000_skins_seed_prices.sql` | 8 righe in `item_prices` (costo uniforme 50) |
| `supabase/tests/pets_skins_schema.test.ts` | Verifica seed, colonna nullable |
| `supabase/migrations/20260824092000_select_pet_skin.sql` | Funzione `select_pet_skin` |
| `supabase/tests/select_pet_skin.test.ts` | Integrazione della funzione |
| `lib/rpc.ts` | Modificato: nuovo codice `skin_not_owned` |
| `features/pets/skins.ts` | Catalogo statico delle 8 skin (filtro CSS + swatch hex, non il costo) |
| `features/pets/skins.test.ts` | Sanity check del catalogo |
| `features/pets/care.ts` | Modificato: `Pet.active_skin: string \| null` |
| `features/pets/queries.ts` | Modificato: `PetsState.ownedSkins`, `fetchPetsState` legge `owned_items`, nuova `selectPetSkin` |
| `features/pets/usePets.ts` | Modificato: osserva anche `owned_items` |
| `features/pets/usePets.test.ts` | Modificato: il fixture di stato include `ownedSkins` |
| `features/pets/SpeciesImage.tsx` | Modificato: nuova prop `filter?: string` |
| `features/pets/SpeciesImage.test.tsx` | Modificato: test del filtro |
| `features/pets/SkinPicker.tsx` | Griglia di 8 skin + "Natural", per-pet |
| `features/pets/SkinPicker.test.tsx` | Unit |
| `features/pets/PetCard.tsx` | Modificato: applica il filtro della skin attiva |
| `features/pets/PetCard.test.tsx` | Modificato: nuovo test del filtro |
| `features/pets/PetDetail.tsx` | Modificato: nuova prop `prices`, sezione Skins |
| `features/pets/PetDetail.test.tsx` | Modificato: nuove prop nei render, nuovo test |
| `features/pets/pets.module.css` | Modificato: classi della griglia skin |
| `app/pets/[species]/page.tsx` | Modificato: passa `prices`/`ownedSkins` a `PetDetail` |
| `app/pets/[species]/page.test.tsx` | Modificato: fixture aggiornato |
| `docs/schema-f4.2-skin-animali.sql` | Doc di deploy in produzione |

---

### Task 1: Schema — colonna `active_skin` e catalogo prezzi delle skin

**Files:**
- Create: `supabase/migrations/20260824090000_pets_active_skin.sql`
- Create: `supabase/migrations/20260824091000_skins_seed_prices.sql`
- Create: `supabase/tests/pets_skins_schema.test.ts`

**Interfaces:**
- Consumes: tabella `pets` (F4.1), tabella `item_prices` (F0+F1).
- Produces: colonna `pets.active_skin text null`; 8 righe in `item_prices` con chiavi `skin_gold`, `skin_ocean`, `skin_sunset`, `skin_forest`, `skin_rose`, `skin_mint`, `skin_violet`, `skin_charcoal`, costo 50 ciascuna. Consumati da Task 2 (`select_pet_skin` legge `owned_items`/`pets`) e Task 3 (`skins.ts` usa le stesse chiavi).

- [ ] **Step 1: Scrivi il test che deve fallire**

`supabase/tests/pets_skins_schema.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { sql } from './helpers';

describe('schema delle skin', () => {
  it('semina esattamente 8 skin in item_prices', async () => {
    const rows = await sql<{ count: string }>(
      `select count(*) from item_prices where key like 'skin_%'`,
    );
    expect(Number(rows[0].count)).toBe(8);
  });

  it('applica il costo uniforme di 50 monete', async () => {
    const rows = await sql<{ cost: number }>(`select cost from item_prices where key like 'skin_%'`);
    expect(rows.length).toBeGreaterThan(0);
    expect(rows.every((r) => r.cost === 50)).toBe(true);
  });

  it('pets ha la colonna active_skin, nullable', async () => {
    const rows = await sql<{ is_nullable: string }>(`
      select is_nullable from information_schema.columns
      where table_name = 'pets' and column_name = 'active_skin'
    `);
    expect(rows).toHaveLength(1);
    expect(rows[0].is_nullable).toBe('YES');
  });
});
```

- [ ] **Step 2: Esegui e verifica che fallisca**

Run: `npm run test:int -- pets_skins_schema`
Expected: FAIL — la colonna `active_skin` non esiste, `item_prices` non ha righe `skin_%`.

- [ ] **Step 3: Scrivi `supabase/migrations/20260824090000_pets_active_skin.sql`**

```sql
-- Colore attivo su questo animale/pianta, o null per il colore naturale.
-- Nessuna FK verso item_prices: la vera regola non è "la chiave esiste"
-- ma "è stata comprata" (owned_items), un controllo che vive nella
-- funzione select_pet_skin (Task 2), non nello schema — stesso principio
-- già in vigore per select_theme (F6).
alter table pets add column active_skin text null;
```

- [ ] **Step 4: Scrivi `supabase/migrations/20260824091000_skins_seed_prices.sql`**

```sql
-- Palette globale di 8 colori, riusabile su qualunque animale/pianta
-- posseduto (spec F4.2, sezione 6). Costo uniforme, più economico dei
-- temi di Shop (100) essendo una personalizzazione più piccola.
insert into item_prices (key, cost, label) values
  ('skin_gold', 50, 'Gold'),
  ('skin_ocean', 50, 'Ocean blue'),
  ('skin_sunset', 50, 'Sunset orange'),
  ('skin_forest', 50, 'Forest green'),
  ('skin_rose', 50, 'Rose pink'),
  ('skin_mint', 50, 'Mint'),
  ('skin_violet', 50, 'Violet'),
  ('skin_charcoal', 50, 'Charcoal');
```

- [ ] **Step 5: Applica e verifica**

Run: `npm run db:reset && npm run test:int -- pets_skins_schema`
Expected: 3 test PASS.

- [ ] **Step 6: Verifica che il resto della suite di integrazione sia ancora verde**

Run: `npm run test:int`
Expected: tutti i test PASS — nessuna regressione su Shop/Pets/Giochi/Domande (che condividono `item_prices`).

- [ ] **Step 7: Rigenera i tipi**

Run: `npm run db:types`

- [ ] **Step 8: Commit**

```bash
git add supabase lib/types.ts
git commit -m "feat: colonna active_skin e catalogo prezzi delle 8 skin"
```

### Task 2: La funzione `select_pet_skin`

**Files:**
- Create: `supabase/migrations/20260824092000_select_pet_skin.sql`
- Create: `supabase/tests/select_pet_skin.test.ts`
- Modify: `lib/rpc.ts`

**Interfaces:**
- Consumes: tabella `pets`, `owned_items` (F4.1/F6), funzione `unlock_pet`/`purchase_item` esistenti (usate solo nei test come fixture).
- Produces: `select_pet_skin(p_species_key text, p_skin_key text) returns pets`. Errori: `skin_not_owned` (nuovo), `pet_not_found` (F4.1, riusato senza modifiche). Consumati da `features/pets/queries.ts` (Task 3).

- [ ] **Step 1: Scrivi il test che deve fallire**

`supabase/tests/select_pet_skin.test.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { sql, resetData } from './helpers';

const setCoins = (n: number) => sql('update couple_state set coins = $1 where id = 1', [n]);

const unlock = (key: string, kind: string, stats: object) =>
  sql('select unlock_pet($1::person, $2, $3::pet_kind, $4::jsonb)', [
    'emily',
    key,
    kind,
    JSON.stringify(stats),
  ]);

const purchase = (key: string) => sql('select purchase_item($1::person, $2)', ['emily', key]);

const selectSkin = (speciesKey: string, skinKey: string | null) =>
  sql('select select_pet_skin($1, $2)', [speciesKey, skinKey]);

const pet = async (key: string) =>
  (await sql<{ active_skin: string | null }>('select * from pets where species_key = $1', [key]))[0];

beforeEach(async () => {
  await resetData();
  await setCoins(1000);
  await unlock('pet_dog', 'animal', { hunger: 100, cleanliness: 100, affection: 100 });
});

describe('select_pet_skin', () => {
  it('attiva una skin posseduta', async () => {
    await purchase('skin_gold');
    await selectSkin('pet_dog', 'skin_gold');
    expect((await pet('pet_dog')).active_skin).toBe('skin_gold');
  });

  it('rifiuta una skin non posseduta', async () => {
    await expect(selectSkin('pet_dog', 'skin_gold')).rejects.toThrow(/skin_not_owned/);
  });

  it('propaga pet_not_found per una specie non sbloccata', async () => {
    await expect(selectSkin('pet_cat', 'skin_gold')).rejects.toThrow(/pet_not_found/);
  });

  it('torna al colore naturale con null, sempre permesso', async () => {
    await purchase('skin_gold');
    await selectSkin('pet_dog', 'skin_gold');
    await selectSkin('pet_dog', null);
    expect((await pet('pet_dog')).active_skin).toBeNull();
  });

  it('è eseguibile da authenticated ma non da anon', async () => {
    const [row] = await sql<{ can_authenticated: boolean; can_anon: boolean }>(`
      select
        has_function_privilege('authenticated', 'public.select_pet_skin(text, text)', 'EXECUTE') as can_authenticated,
        has_function_privilege('anon', 'public.select_pet_skin(text, text)', 'EXECUTE') as can_anon
    `);
    expect(row.can_authenticated).toBe(true);
    expect(row.can_anon).toBe(false);
  });
});
```

- [ ] **Step 2: Esegui e verifica che falliscano**

Run: `npm run test:int -- select_pet_skin`
Expected: FAIL — la funzione non esiste.

- [ ] **Step 3: Scrivi `supabase/migrations/20260824092000_select_pet_skin.sql`**

```sql
-- Attivare una skin posseduta è gratis, sempre — comprare (purchase_item)
-- e attivare restano due passi distinti, stesso principio di
-- select_theme (F6). p_skin_key null riporta l'animale al colore
-- naturale.
create or replace function select_pet_skin(
  p_species_key text,
  p_skin_key    text
) returns pets
language plpgsql
security definer
set search_path = public
as $$
declare
  v_pet pets;
begin
  if not exists (select 1 from pets where species_key = p_species_key) then
    raise exception 'pet_not_found';
  end if;

  if p_skin_key is not null and not exists (
    select 1 from owned_items where key = p_skin_key
  ) then
    raise exception 'skin_not_owned';
  end if;

  update pets
     set active_skin = p_skin_key
   where species_key = p_species_key
  returning * into v_pet;

  return v_pet;
end
$$;

revoke all on function select_pet_skin(text, text) from public, anon;
grant execute on function select_pet_skin(text, text) to authenticated;
```

- [ ] **Step 4: Applica e verifica**

Run: `npm run db:reset && npm run test:int -- select_pet_skin`
Expected: 5 test PASS.

- [ ] **Step 5: Verifica che il resto della suite di integrazione sia ancora verde**

Run: `npm run test:int`
Expected: tutti i test PASS.

- [ ] **Step 6: Rigenera i tipi**

Run: `npm run db:types`

- [ ] **Step 7: Aggiungi il nuovo codice d'errore a `lib/rpc.ts`**

Nell'array `MESSAGES`, aggiungi una riga (dopo `'invalid_pet_name'` va bene):

```ts
  ['skin_not_owned', "You don't own that skin yet."],
```

Nessun'altra modifica al file.

- [ ] **Step 8: Verifica l'intera suite unit**

Run: `npm run test`
Expected: tutti i test PASS.

- [ ] **Step 9: Commit**

```bash
git add supabase lib/types.ts lib/rpc.ts
git commit -m "feat: funzione select_pet_skin"
```

### Task 3: Catalogo delle skin e data layer frontend

**Files:**
- Create: `features/pets/skins.ts`
- Create: `features/pets/skins.test.ts`
- Modify: `features/pets/care.ts`
- Modify: `features/pets/queries.ts`
- Modify: `features/pets/usePets.ts`
- Modify: `features/pets/usePets.test.ts`

**Interfaces:**
- Consumes: nessuna dipendenza da task precedenti per `skins.ts`; `Pet` (definito in `care.ts`, F4.1) per `queries.ts`.
- Produces: `type SkinKey`, `type Skin = { key: SkinKey; label: string; filter: string; swatch: string }`, `SKINS: Skin[]` (8 elementi), `skinFilterFor(activeSkin: string | null | undefined): string | undefined` — tutti in `features/pets/skins.ts`. `Pet.active_skin: string | null` (aggiunto al tipo esistente). `PetsState.ownedSkins: string[]` (aggiunto). `selectPetSkin(speciesKey: string, skinKey: string | null, client?): Promise<{data, error}>`. Consumati da Task 4.

- [ ] **Step 1: Scrivi `features/pets/skins.ts`**

```ts
export type SkinKey =
  | 'skin_gold'
  | 'skin_ocean'
  | 'skin_sunset'
  | 'skin_forest'
  | 'skin_rose'
  | 'skin_mint'
  | 'skin_violet'
  | 'skin_charcoal';

export type Skin = {
  key: SkinKey;
  label: string;
  /** Applicato all'emoji/immagine della specie con style={{ filter }}. */
  filter: string;
  /** Solo per l'anteprima del picker (cerchio colorato) — non è il colore reale della creatura. */
  swatch: string;
};

/**
 * Catalogo statico: chiave, etichetta, filtro e swatch di anteprima. Il
 * costo NON vive qui — arriva da item_prices via usePets, fonte unica di
 * verità sul prezzo (stesso principio di features/shop/themes.ts e
 * features/pets/species.ts).
 */
export const SKINS: Skin[] = [
  { key: 'skin_gold', label: 'Gold', filter: 'hue-rotate(35deg) saturate(1.6) brightness(1.1)', swatch: '#d4a12a' },
  { key: 'skin_ocean', label: 'Ocean blue', filter: 'hue-rotate(190deg) saturate(1.5)', swatch: '#1f7ac2' },
  { key: 'skin_sunset', label: 'Sunset orange', filter: 'hue-rotate(-20deg) saturate(1.5) brightness(1.05)', swatch: '#e0672f' },
  { key: 'skin_forest', label: 'Forest green', filter: 'hue-rotate(90deg) saturate(1.4)', swatch: '#3f7a3d' },
  { key: 'skin_rose', label: 'Rose pink', filter: 'hue-rotate(300deg) saturate(1.5)', swatch: '#d1487a' },
  { key: 'skin_mint', label: 'Mint', filter: 'hue-rotate(140deg) saturate(1.3) brightness(1.1)', swatch: '#3fbf9f' },
  { key: 'skin_violet', label: 'Violet', filter: 'hue-rotate(250deg) saturate(1.5)', swatch: '#7a4fc2' },
  { key: 'skin_charcoal', label: 'Charcoal', filter: 'grayscale(1) brightness(0.75)', swatch: '#4a4a4a' },
];

/** Il filtro CSS della skin attiva, o undefined se nessuna (colore naturale). */
export function skinFilterFor(activeSkin: string | null | undefined): string | undefined {
  return SKINS.find((s) => s.key === activeSkin)?.filter;
}
```

- [ ] **Step 2: Scrivi `features/pets/skins.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { SKINS, skinFilterFor } from './skins';

describe('catalogo delle skin', () => {
  it('contiene esattamente 8 skin', () => {
    expect(SKINS).toHaveLength(8);
  });

  it('ha chiavi tutte diverse', () => {
    expect(new Set(SKINS.map((s) => s.key)).size).toBe(SKINS.length);
  });

  it('ogni skin ha un filtro CSS e uno swatch non vuoti', () => {
    for (const skin of SKINS) {
      expect(skin.filter.trim().length).toBeGreaterThan(0);
      expect(skin.swatch.trim().length).toBeGreaterThan(0);
    }
  });
});

describe('skinFilterFor', () => {
  it('ritorna il filtro della skin attiva', () => {
    expect(skinFilterFor('skin_gold')).toBe(SKINS[0].filter);
  });

  it('ritorna undefined per null/undefined/chiave sconosciuta', () => {
    expect(skinFilterFor(null)).toBeUndefined();
    expect(skinFilterFor(undefined)).toBeUndefined();
    expect(skinFilterFor('nope')).toBeUndefined();
  });
});
```

- [ ] **Step 3: Esegui e verifica che i test passino**

Run: `npm run test -- features/pets/skins`
Expected: 5 test PASS.

- [ ] **Step 4: Aggiungi `active_skin` al tipo `Pet` in `features/pets/care.ts`**

Cambia:

```ts
export type Pet = {
  species_key: string;
  kind: PetKind;
  nickname: string | null;
  stats: Record<string, number>;
  updated_at: string;
  unlocked_at: string;
};
```

in:

```ts
export type Pet = {
  species_key: string;
  kind: PetKind;
  nickname: string | null;
  stats: Record<string, number>;
  updated_at: string;
  unlocked_at: string;
  active_skin: string | null;
};
```

Nessun'altra modifica al file: `projectStats`/`applyCareAction`/`needsAttention` non toccano `active_skin`.

- [ ] **Step 5: Verifica che la suite unit di `care.ts` sia ancora verde**

Run: `npm run test -- features/pets/care`
Expected: tutti PASS (l'aggiunta di un campo è un allargamento del tipo, non rompe nulla).

- [ ] **Step 6: Modifica `features/pets/queries.ts`**

Il file attuale è:

```ts
import type { SupabaseClient } from '@supabase/supabase-js';
import { getSupabase } from '@/lib/supabase/client';
import { call } from '@/lib/rpc';
import type { Database } from '@/lib/types';
import type { Person } from '@/features/auth/identity';
import type { Pet } from './care';
import type { PetKind } from './species';

export type PetsState = {
  /** item_prices per le chiavi pet_ / plant_: costo di sblocco. */
  prices: Record<string, number>;
  /** Specie sbloccate dalla coppia, con statistiche e nickname correnti. */
  pets: Pet[];
};

type Client = SupabaseClient<Database>;
const db = (client?: Client): Client => client ?? getSupabase();

export async function fetchPetsState(client?: Client): Promise<PetsState> {
  const c = db(client);

  const { data: prices, error: pricesError } = await c.from('item_prices').select('key, cost');
  if (pricesError) throw new Error(pricesError.message);

  const { data: pets, error: petsError } = await c.from('pets').select('*');
  if (petsError) throw new Error(petsError.message);

  return {
    prices: Object.fromEntries((prices ?? []).map((p) => [p.key, p.cost])),
    pets: (pets ?? []) as Pet[],
  };
}

export async function unlockPet(
  actor: Person,
  speciesKey: string,
  kind: PetKind,
  initialStats: Record<string, number>,
  client?: Client,
) {
  return call(
    db(client).rpc('unlock_pet', {
      p_actor: actor,
      p_species_key: speciesKey,
      p_kind: kind,
      p_initial_stats: initialStats,
    }),
  );
}

export async function careForPet(
  actor: Person,
  speciesKey: string,
  stats: Record<string, number>,
  client?: Client,
) {
  return call(
    db(client).rpc('care_for_pet', { p_actor: actor, p_species_key: speciesKey, p_stats: stats }),
  );
}

export async function renamePet(speciesKey: string, name: string, client?: Client) {
  return call(db(client).rpc('rename_pet', { p_species_key: speciesKey, p_name: name }));
}
```

Sostituiscilo con:

```ts
import type { SupabaseClient } from '@supabase/supabase-js';
import { getSupabase } from '@/lib/supabase/client';
import { call } from '@/lib/rpc';
import type { Database } from '@/lib/types';
import type { Person } from '@/features/auth/identity';
import type { Pet } from './care';
import type { PetKind } from './species';

export type PetsState = {
  /** item_prices per le chiavi pet_ / plant_ / skin_: costo di sblocco. */
  prices: Record<string, number>;
  /** Specie sbloccate dalla coppia, con statistiche e nickname correnti. */
  pets: Pet[];
  /** Chiavi skin_* possedute dalla coppia. */
  ownedSkins: string[];
};

type Client = SupabaseClient<Database>;
const db = (client?: Client): Client => client ?? getSupabase();

export async function fetchPetsState(client?: Client): Promise<PetsState> {
  const c = db(client);

  const { data: prices, error: pricesError } = await c.from('item_prices').select('key, cost');
  if (pricesError) throw new Error(pricesError.message);

  const { data: pets, error: petsError } = await c.from('pets').select('*');
  if (petsError) throw new Error(petsError.message);

  const { data: ownedItems, error: ownedError } = await c
    .from('owned_items')
    .select('key')
    .like('key', 'skin_%');
  if (ownedError) throw new Error(ownedError.message);

  return {
    prices: Object.fromEntries((prices ?? []).map((p) => [p.key, p.cost])),
    pets: (pets ?? []) as Pet[],
    ownedSkins: (ownedItems ?? []).map((o) => o.key),
  };
}

export async function unlockPet(
  actor: Person,
  speciesKey: string,
  kind: PetKind,
  initialStats: Record<string, number>,
  client?: Client,
) {
  return call(
    db(client).rpc('unlock_pet', {
      p_actor: actor,
      p_species_key: speciesKey,
      p_kind: kind,
      p_initial_stats: initialStats,
    }),
  );
}

export async function careForPet(
  actor: Person,
  speciesKey: string,
  stats: Record<string, number>,
  client?: Client,
) {
  return call(
    db(client).rpc('care_for_pet', { p_actor: actor, p_species_key: speciesKey, p_stats: stats }),
  );
}

export async function renamePet(speciesKey: string, name: string, client?: Client) {
  return call(db(client).rpc('rename_pet', { p_species_key: speciesKey, p_name: name }));
}

export async function selectPetSkin(speciesKey: string, skinKey: string | null, client?: Client) {
  return call(
    db(client).rpc('select_pet_skin', { p_species_key: speciesKey, p_skin_key: skinKey }),
  );
}
```

- [ ] **Step 7: Modifica `features/pets/usePets.ts`**

Il file attuale è:

```ts
'use client';

import type { SupabaseClient } from '@supabase/supabase-js';
import { useRealtimeQuery } from '@/lib/useRealtimeQuery';
import { fetchPetsState, type PetsState } from './queries';

export function usePets(options: { client?: SupabaseClient } = {}) {
  return useRealtimeQuery<PetsState>({
    // item_prices non è fra le tabelle osservate: è seme statico, come già
    // per Shop (F6) — i costi cambiano solo via migrazione + deploy.
    tables: ['pets'],
    client: options.client,
    fetcher: () => fetchPetsState(options.client),
  });
}
```

Sostituiscilo con:

```ts
'use client';

import type { SupabaseClient } from '@supabase/supabase-js';
import { useRealtimeQuery } from '@/lib/useRealtimeQuery';
import { fetchPetsState, type PetsState } from './queries';

export function usePets(options: { client?: SupabaseClient } = {}) {
  return useRealtimeQuery<PetsState>({
    // item_prices non è osservata: è seme statico, come già per Shop (F6).
    // owned_items invece sì (F4.2): un acquisto di skin scrive lì, non in
    // pets, e deve comunque innescare un refetch che mostri il nuovo
    // colore disponibile nel picker.
    tables: ['pets', 'owned_items'],
    client: options.client,
    fetcher: () => fetchPetsState(options.client),
  });
}
```

- [ ] **Step 8: Aggiorna `features/pets/usePets.test.ts`**

Trova la funzione `state`:

```ts
const state = (over: Partial<PetsState> = {}): PetsState => ({
  prices: { pet_dog: 35 },
  pets: [],
  ...over,
});
```

Sostituiscila con:

```ts
const state = (over: Partial<PetsState> = {}): PetsState => ({
  prices: { pet_dog: 35 },
  pets: [],
  ownedSkins: [],
  ...over,
});
```

Nessun'altra modifica al file.

- [ ] **Step 9: Esegui e verifica che i test passino**

Run: `npm run test -- features/pets`
Expected: tutti PASS.

- [ ] **Step 10: Verifica l'intera suite unit**

Run: `npm run test`
Expected: tutti i test PASS.

- [ ] **Step 11: Commit**

```bash
git add features/pets/skins.ts features/pets/skins.test.ts features/pets/care.ts features/pets/queries.ts features/pets/usePets.ts features/pets/usePets.test.ts
git commit -m "feat: catalogo delle skin e data layer (ownedSkins, selectPetSkin)"
```

### Task 4: UI — filtro colore, picker e integrazione nelle schermate

**Files:**
- Modify: `features/pets/SpeciesImage.tsx`
- Modify: `features/pets/SpeciesImage.test.tsx`
- Modify: `features/pets/pets.module.css`
- Create: `features/pets/SkinPicker.tsx`
- Create: `features/pets/SkinPicker.test.tsx`
- Modify: `features/pets/PetCard.tsx`
- Modify: `features/pets/PetCard.test.tsx`
- Modify: `features/pets/PetDetail.tsx`
- Modify: `features/pets/PetDetail.test.tsx`
- Modify: `app/pets/[species]/page.tsx`
- Modify: `app/pets/[species]/page.test.tsx`

**Interfaces:**
- Consumes: `SKINS`, `skinFilterFor` (Task 3); `selectPetSkin` (Task 3); `purchaseItem` (`@/features/shop/queries`, F6, invariato); `Pet.active_skin` (Task 3).
- Produces: `<SpeciesImage ... filter?: string />`; `<SkinPicker speciesKey activeSkin ownedSkins prices who />`; `PetDetail` con nuova prop `prices: Record<string, number>` al posto del solo `cost` isolato (deriva `cost` internamente).

- [ ] **Step 1: Modifica `features/pets/SpeciesImage.tsx`**

Il file attuale è:

```tsx
'use client';

import { useState } from 'react';

/**
 * L'immagine attesa vive in public/pets/<speciesKey>.png (fornita dall'
 * utente, non generata). Se il file manca (404), l'onError sostituisce con
 * l'emoji del catalogo — l'app funziona da subito, ogni file caricato col
 * nome giusto sostituisce l'emoji senza toccare codice.
 */
export function SpeciesImage({
  speciesKey,
  emoji,
  alt,
  className,
}: {
  speciesKey: string;
  emoji: string;
  alt: string;
  className?: string;
}) {
  const [failed, setFailed] = useState(false);

  if (failed) {
    return (
      <span className={className} role="img" aria-label={alt}>
        {emoji}
      </span>
    );
  }

  return (
    <img
      src={`/pets/${speciesKey}.png`}
      alt={alt}
      className={className}
      onError={() => setFailed(true)}
    />
  );
}
```

Sostituiscilo con:

```tsx
'use client';

import { useState } from 'react';

/**
 * L'immagine attesa vive in public/pets/<speciesKey>.png (fornita dall'
 * utente, non generata). Se il file manca (404), l'onError sostituisce con
 * l'emoji del catalogo — l'app funziona da subito, ogni file caricato col
 * nome giusto sostituisce l'emoji senza toccare codice. Il filtro CSS di
 * un'eventuale skin attiva (F4.2) si applica identico su entrambi i rami.
 */
export function SpeciesImage({
  speciesKey,
  emoji,
  alt,
  className,
  filter,
}: {
  speciesKey: string;
  emoji: string;
  alt: string;
  className?: string;
  filter?: string;
}) {
  const [failed, setFailed] = useState(false);
  const style = filter ? { filter } : undefined;

  if (failed) {
    return (
      <span className={className} style={style} role="img" aria-label={alt}>
        {emoji}
      </span>
    );
  }

  return (
    <img
      src={`/pets/${speciesKey}.png`}
      alt={alt}
      className={className}
      style={style}
      onError={() => setFailed(true)}
    />
  );
}
```

- [ ] **Step 2: Aggiorna `features/pets/SpeciesImage.test.tsx`**

Il file attuale è:

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { SpeciesImage } from './SpeciesImage';

describe('SpeciesImage', () => {
  it("mostra l'immagine della specie", () => {
    render(<SpeciesImage speciesKey="pet_dog" emoji="🐶" alt="Dog" />);
    const img = screen.getByRole('img', { name: 'Dog' }) as HTMLImageElement;
    expect(img.src).toContain('/pets/pet_dog.png');
  });

  it("mostra l'emoji se l'immagine non carica", () => {
    render(<SpeciesImage speciesKey="pet_dog" emoji="🐶" alt="Dog" />);
    fireEvent.error(screen.getByRole('img', { name: 'Dog' }));
    expect(screen.getByText('🐶')).toBeDefined();
  });
});
```

Aggiungi due test in fondo, prima della chiusura di `describe`:

```tsx
  it('applica il filtro CSS quando passato, sia su immagine sia su emoji fallback', () => {
    render(<SpeciesImage speciesKey="pet_dog" emoji="🐶" alt="Dog" filter="hue-rotate(35deg)" />);
    const img = screen.getByRole('img', { name: 'Dog' }) as HTMLImageElement;
    expect(img.style.filter).toBe('hue-rotate(35deg)');

    fireEvent.error(img);
    const fallback = screen.getByText('🐶');
    expect(fallback.style.filter).toBe('hue-rotate(35deg)');
  });

  it('nessun filtro applicato quando la prop non è passata', () => {
    render(<SpeciesImage speciesKey="pet_dog" emoji="🐶" alt="Dog" />);
    const img = screen.getByRole('img', { name: 'Dog' }) as HTMLImageElement;
    expect(img.style.filter).toBe('');
  });
```

- [ ] **Step 3: Esegui e verifica che i test passino**

Run: `npm run test -- features/pets/SpeciesImage`
Expected: 4 test PASS.

- [ ] **Step 4: Estendi `features/pets/pets.module.css`**

Aggiungi in fondo al file (non toccare le regole esistenti):

```css
.skinGrid { display: grid; grid-template-columns: repeat(auto-fill, minmax(90px, 1fr)); gap: var(--space-2); }
.skinSwatch {
  display: grid;
  justify-items: center;
  gap: var(--space-1);
  min-height: 44px;
  padding: var(--space-2);
  border-radius: var(--radius-md);
  border: 1px solid var(--line);
  background: var(--surface);
  font: var(--font-small);
}
.skinSwatch:disabled { opacity: 0.6; }
.skinSwatch.skinActive { border-color: var(--accent); border-width: 2px; }
.skinDot { width: 20px; height: 20px; border-radius: 50%; display: block; }
```

- [ ] **Step 5: Scrivi `features/pets/SkinPicker.tsx`**

```tsx
'use client';

import { useRef, useState } from 'react';
import type { Person } from '@/features/auth/identity';
import { purchaseItem } from '@/features/shop/queries';
import { selectPetSkin } from './queries';
import { SKINS } from './skins';
import styles from './pets.module.css';

export function SkinPicker({
  speciesKey,
  activeSkin,
  ownedSkins,
  prices,
  who,
}: {
  speciesKey: string;
  activeSkin: string | null;
  ownedSkins: string[];
  prices: Record<string, number>;
  who: Person;
}) {
  const [busyKey, setBusyKey] = useState<string | null>(null);
  const [error, setError] = useState<string | null>(null);
  // Stessa guardia sincrona delle altre azioni della card: il `disabled`
  // da solo non basta fra due tocchi molto ravvicinati.
  const sending = useRef(false);

  async function activate(skinKey: string | null) {
    if (sending.current) return;
    sending.current = true;
    setBusyKey(skinKey ?? 'natural');
    setError(null);
    const { error: failure } = await selectPetSkin(speciesKey, skinKey);
    setBusyKey(null);
    sending.current = false;
    if (failure) setError(failure);
  }

  async function buyAndActivate(skinKey: string) {
    if (sending.current) return;
    sending.current = true;
    setBusyKey(skinKey);
    setError(null);
    const { error: purchaseFailure } = await purchaseItem(who, skinKey);
    if (purchaseFailure) {
      setBusyKey(null);
      sending.current = false;
      setError(purchaseFailure);
      return;
    }
    const { error: activateFailure } = await selectPetSkin(speciesKey, skinKey);
    setBusyKey(null);
    sending.current = false;
    if (activateFailure) setError(activateFailure);
  }

  return (
    <div>
      <p className={styles.sectionTitle}>Skins</p>
      <div className={styles.skinGrid}>
        <button
          type="button"
          className={`${styles.skinSwatch} ${activeSkin === null ? styles.skinActive : ''}`}
          onClick={() => activate(null)}
          disabled={busyKey !== null}
        >
          <span className={styles.skinDot} style={{ background: 'var(--fg-muted)' }} aria-hidden />
          Natural
        </button>
        {SKINS.map((skin) => {
          const owned = ownedSkins.includes(skin.key);
          const active = activeSkin === skin.key;
          const cost = prices[skin.key];
          const label =
            owned || cost === undefined ? skin.label : `${skin.label} · ${cost}`;
          return (
            <button
              key={skin.key}
              type="button"
              className={`${styles.skinSwatch} ${active ? styles.skinActive : ''}`}
              onClick={() => (owned ? activate(skin.key) : buyAndActivate(skin.key))}
              disabled={busyKey !== null || (!owned && cost === undefined)}
            >
              <span className={styles.skinDot} style={{ background: skin.swatch }} aria-hidden />
              {busyKey === skin.key ? '…' : label}
            </button>
          );
        })}
      </div>
      {error && (
        <p className={styles.error} role="alert">
          {error}
        </p>
      )}
    </div>
  );
}
```

- [ ] **Step 6: Scrivi `features/pets/SkinPicker.test.tsx`**

```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import { SkinPicker } from './SkinPicker';

const purchaseItem = vi.fn();
vi.mock('@/features/shop/queries', () => ({ purchaseItem: (...a: unknown[]) => purchaseItem(...a) }));

const selectPetSkin = vi.fn();
vi.mock('./queries', () => ({ selectPetSkin: (...a: unknown[]) => selectPetSkin(...a) }));

describe('SkinPicker', () => {
  beforeEach(() => {
    purchaseItem.mockReset();
    selectPetSkin.mockReset();
    purchaseItem.mockResolvedValue({ data: null, error: null });
    selectPetSkin.mockResolvedValue({ data: null, error: null });
  });

  it('skin non posseduta: mostra il prezzo; comprando, acquista e poi attiva', async () => {
    render(
      <SkinPicker speciesKey="pet_dog" activeSkin={null} ownedSkins={[]} prices={{ skin_gold: 50 }} who="fabrizio" />,
    );
    screen.getByRole('button', { name: /Gold · 50/ }).click();
    await waitFor(() => expect(purchaseItem).toHaveBeenCalledWith('fabrizio', 'skin_gold'));
    await waitFor(() => expect(selectPetSkin).toHaveBeenCalledWith('pet_dog', 'skin_gold'));
  });

  it('skin posseduta ma non attiva: attivando, chiama solo selectPetSkin', async () => {
    render(
      <SkinPicker speciesKey="pet_dog" activeSkin={null} ownedSkins={['skin_gold']} prices={{ skin_gold: 50 }} who="emily" />,
    );
    screen.getByRole('button', { name: 'Gold' }).click();
    await waitFor(() => expect(selectPetSkin).toHaveBeenCalledWith('pet_dog', 'skin_gold'));
    expect(purchaseItem).not.toHaveBeenCalled();
  });

  it('Natural è sempre attivabile, anche senza alcuna skin posseduta', async () => {
    render(
      <SkinPicker speciesKey="pet_dog" activeSkin="skin_gold" ownedSkins={['skin_gold']} prices={{ skin_gold: 50 }} who="fabrizio" />,
    );
    screen.getByRole('button', { name: 'Natural' }).click();
    await waitFor(() => expect(selectPetSkin).toHaveBeenCalledWith('pet_dog', null));
  });

  it('acquisto fallito per monete insufficienti: mostra l’errore, non attiva', async () => {
    purchaseItem.mockResolvedValue({ data: null, error: "You don't have enough coins for that yet." });
    render(
      <SkinPicker speciesKey="pet_dog" activeSkin={null} ownedSkins={[]} prices={{ skin_gold: 50 }} who="fabrizio" />,
    );
    screen.getByRole('button', { name: /Gold · 50/ }).click();
    await waitFor(() => expect(screen.getByRole('alert')).toBeDefined());
    expect(selectPetSkin).not.toHaveBeenCalled();
  });

  it('due tocchi rapidi sulla stessa skin inviano una sola attivazione', async () => {
    render(
      <SkinPicker speciesKey="pet_dog" activeSkin={null} ownedSkins={['skin_gold']} prices={{ skin_gold: 50 }} who="fabrizio" />,
    );
    const button = screen.getByRole('button', { name: 'Gold' });
    button.click();
    button.click();
    await waitFor(() => expect(selectPetSkin).toHaveBeenCalled());
    expect(selectPetSkin).toHaveBeenCalledTimes(1);
  });

  it('costo non ancora caricato: il pulsante mostra solo il nome, disabilitato', () => {
    render(<SkinPicker speciesKey="pet_dog" activeSkin={null} ownedSkins={[]} prices={{}} who="fabrizio" />);
    const button = screen.getByRole('button', { name: 'Gold' });
    expect((button as HTMLButtonElement).disabled).toBe(true);
  });
});
```

- [ ] **Step 7: Esegui e verifica che i test passino**

Run: `npm run test -- features/pets/SkinPicker`
Expected: 6 test PASS.

- [ ] **Step 8: Modifica `features/pets/PetCard.tsx`**

Aggiungi l'import e applica il filtro nella parte sbloccata. Cambia:

```tsx
import { unlockPet } from './queries';
import { SpeciesImage } from './SpeciesImage';
import { useNow } from './useNow';
import styles from './pets.module.css';
```

in:

```tsx
import { unlockPet } from './queries';
import { SpeciesImage } from './SpeciesImage';
import { skinFilterFor } from './skins';
import { useNow } from './useNow';
import styles from './pets.module.css';
```

e cambia:

```tsx
      <SpeciesImage speciesKey={species.key} emoji={species.emoji} alt={pet.nickname ?? species.name} className={styles.image} />
```

in:

```tsx
      <SpeciesImage
        speciesKey={species.key}
        emoji={species.emoji}
        alt={pet.nickname ?? species.name}
        className={styles.image}
        filter={skinFilterFor(pet.active_skin)}
      />
```

Nessun'altra modifica al file (la card bloccata non ha `active_skin`, resta invariata).

- [ ] **Step 9: Aggiungi un test a `features/pets/PetCard.test.tsx`**

Aggiungi in fondo al file, prima della chiusura di `describe`:

```tsx
  it('sbloccata con una skin attiva: applica il filtro CSS corrispondente', () => {
    const pet: Pet = {
      species_key: 'pet_dog', kind: 'animal', nickname: null,
      stats: { hunger: 100, cleanliness: 100, affection: 100 },
      updated_at: new Date().toISOString(), unlocked_at: new Date().toISOString(),
      active_skin: 'skin_gold',
    };
    render(<PetCard species={dog} pet={pet} cost={35} who="fabrizio" />);
    const img = screen.getByRole('img') as HTMLImageElement;
    expect(img.style.filter).toBe('hue-rotate(35deg) saturate(1.6) brightness(1.1)');
  });
```

I due oggetti `Pet` già esistenti più sopra nel file vanno completati con
`active_skin: null` (il tipo `Pet` ora lo richiede). Nel test "sbloccata:
mostra il nickname e linka al dettaglio", cambia:

```ts
    const pet: Pet = {
      species_key: 'pet_dog', kind: 'animal', nickname: 'Rex',
      stats: { hunger: 100, cleanliness: 100, affection: 100 },
      updated_at: '2020-08-23T10:00:00Z', unlocked_at: '2020-08-23T10:00:00Z',
    };
```

in:

```ts
    const pet: Pet = {
      species_key: 'pet_dog', kind: 'animal', nickname: 'Rex',
      stats: { hunger: 100, cleanliness: 100, affection: 100 },
      updated_at: '2020-08-23T10:00:00Z', unlocked_at: '2020-08-23T10:00:00Z',
      active_skin: null,
    };
```

Nel test "sbloccata e bisognosa di attenzioni: mostra il badge", cambia:

```ts
    const pet: Pet = {
      species_key: 'pet_dog', kind: 'animal', nickname: null,
      stats: { hunger: 10, cleanliness: 100, affection: 100 },
      // Data nel passato remoto (non "oggi ± pochi giorni"): il decadimento
      // usa l'orologio reale via useNow(), quindi la fixture deve restare
      // valida indipendentemente da quando viene eseguito il test.
      updated_at: '2020-08-23T10:00:00Z', unlocked_at: '2020-08-23T10:00:00Z',
    };
```

in:

```ts
    const pet: Pet = {
      species_key: 'pet_dog', kind: 'animal', nickname: null,
      stats: { hunger: 10, cleanliness: 100, affection: 100 },
      // Data nel passato remoto (non "oggi ± pochi giorni"): il decadimento
      // usa l'orologio reale via useNow(), quindi la fixture deve restare
      // valida indipendentemente da quando viene eseguito il test.
      updated_at: '2020-08-23T10:00:00Z', unlocked_at: '2020-08-23T10:00:00Z',
      active_skin: null,
    };
```

- [ ] **Step 10: Esegui e verifica che i test passino**

Run: `npm run test -- features/pets/PetCard`
Expected: 5 test PASS.

- [ ] **Step 11: Modifica `features/pets/PetDetail.tsx`**

Il file attuale è:

```tsx
'use client';

import { useRef, useState } from 'react';
import type { Person } from '@/features/auth/identity';
import type { Species, SpeciesStat } from './species';
import { STAT_LABELS } from './species';
import type { Pet } from './care';
import { initialStatsFor, projectStats, applyCareAction } from './care';
import { unlockPet, careForPet, renamePet } from './queries';
import { useNow } from './useNow';
import { SpeciesImage } from './SpeciesImage';
import styles from './pets.module.css';

export function PetDetail({
  species,
  pet,
  cost,
  who,
}: {
  species: Species;
  pet: Pet | undefined;
  cost: number | undefined;
  who: Person;
}) {
  const now = useNow();
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [nameDraft, setNameDraft] = useState(pet?.nickname ?? '');
  const sending = useRef(false);

  async function unlock() {
    if (sending.current || cost === undefined) return;
    sending.current = true;
    setBusy(true);
    setError(null);
    const { error: failure } = await unlockPet(who, species.key, species.kind, initialStatsFor(species));
    setBusy(false);
    sending.current = false;
    if (failure) setError(failure);
  }

  async function care(stat: SpeciesStat) {
    if (sending.current || !pet) return;
    sending.current = true;
    setBusy(true);
    setError(null);
    const next = applyCareAction(projectStats(pet, species, now), stat);
    const { error: failure } = await careForPet(who, species.key, next);
    setBusy(false);
    sending.current = false;
    if (failure) setError(failure);
  }

  async function rename() {
    if (sending.current || !pet) return;
    sending.current = true;
    setBusy(true);
    setError(null);
    const { error: failure } = await renamePet(species.key, nameDraft);
    setBusy(false);
    sending.current = false;
    if (failure) setError(failure);
  }

  if (!pet) {
    return (
      <div className={styles.detail}>
        <SpeciesImage speciesKey={species.key} emoji={species.emoji} alt={species.name} className={styles.imageLocked} />
        <p className={styles.name}>{species.name}</p>
        <p className={styles.curiosity}>{species.curiosity}</p>
        <button type="button" className={styles.action} onClick={unlock} disabled={busy || cost === undefined}>
          {busy ? 'Unlocking…' : cost === undefined ? 'Unlock' : `Unlock for ${cost} coins`}
        </button>
        {error && (
          <p className={styles.error} role="alert">
            {error}
          </p>
        )}
      </div>
    );
  }

  const stats = projectStats(pet, species, now);
  const statKeys = Object.keys(species.decayPerHour) as SpeciesStat[];

  return (
    <div className={styles.detail}>
      <SpeciesImage
        speciesKey={species.key}
        emoji={species.emoji}
        alt={pet.nickname ?? species.name}
        className={styles.image}
      />
      <div className={styles.renameRow}>
        <input
          className={styles.nameInput}
          value={nameDraft}
          placeholder={species.name}
          onChange={(e) => setNameDraft(e.target.value)}
          maxLength={40}
        />
        <button type="button" className={styles.renameAction} onClick={rename} disabled={busy}>
          Save name
        </button>
      </div>
      <p className={styles.curiosity}>{species.curiosity}</p>
      <div className={styles.stats}>
        {statKeys.map((stat) => (
          <div key={stat} className={styles.statRow}>
            <span className={styles.statLabel}>{stat}</span>
            <span>{Math.round(stats[stat])}</span>
          </div>
        ))}
      </div>
      <div className={styles.actions}>
        {statKeys.map((stat) => (
          <button key={stat} type="button" className={styles.action} onClick={() => care(stat)} disabled={busy}>
            {STAT_LABELS[stat]}
          </button>
        ))}
      </div>
      {error && (
        <p className={styles.error} role="alert">
          {error}
        </p>
      )}
    </div>
  );
}
```

Sostituiscilo con:

```tsx
'use client';

import { useRef, useState } from 'react';
import type { Person } from '@/features/auth/identity';
import type { Species, SpeciesStat } from './species';
import { STAT_LABELS } from './species';
import type { Pet } from './care';
import { initialStatsFor, projectStats, applyCareAction } from './care';
import { unlockPet, careForPet, renamePet } from './queries';
import { skinFilterFor } from './skins';
import { useNow } from './useNow';
import { SpeciesImage } from './SpeciesImage';
import { SkinPicker } from './SkinPicker';
import styles from './pets.module.css';

export function PetDetail({
  species,
  pet,
  prices,
  ownedSkins,
  who,
}: {
  species: Species;
  pet: Pet | undefined;
  prices: Record<string, number>;
  ownedSkins: string[];
  who: Person;
}) {
  const now = useNow();
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [nameDraft, setNameDraft] = useState(pet?.nickname ?? '');
  const sending = useRef(false);
  const cost = prices[species.key];

  async function unlock() {
    if (sending.current || cost === undefined) return;
    sending.current = true;
    setBusy(true);
    setError(null);
    const { error: failure } = await unlockPet(who, species.key, species.kind, initialStatsFor(species));
    setBusy(false);
    sending.current = false;
    if (failure) setError(failure);
  }

  async function care(stat: SpeciesStat) {
    if (sending.current || !pet) return;
    sending.current = true;
    setBusy(true);
    setError(null);
    const next = applyCareAction(projectStats(pet, species, now), stat);
    const { error: failure } = await careForPet(who, species.key, next);
    setBusy(false);
    sending.current = false;
    if (failure) setError(failure);
  }

  async function rename() {
    if (sending.current || !pet) return;
    sending.current = true;
    setBusy(true);
    setError(null);
    const { error: failure } = await renamePet(species.key, nameDraft);
    setBusy(false);
    sending.current = false;
    if (failure) setError(failure);
  }

  if (!pet) {
    return (
      <div className={styles.detail}>
        <SpeciesImage speciesKey={species.key} emoji={species.emoji} alt={species.name} className={styles.imageLocked} />
        <p className={styles.name}>{species.name}</p>
        <p className={styles.curiosity}>{species.curiosity}</p>
        <button type="button" className={styles.action} onClick={unlock} disabled={busy || cost === undefined}>
          {busy ? 'Unlocking…' : cost === undefined ? 'Unlock' : `Unlock for ${cost} coins`}
        </button>
        {error && (
          <p className={styles.error} role="alert">
            {error}
          </p>
        )}
      </div>
    );
  }

  const stats = projectStats(pet, species, now);
  const statKeys = Object.keys(species.decayPerHour) as SpeciesStat[];

  return (
    <div className={styles.detail}>
      <SpeciesImage
        speciesKey={species.key}
        emoji={species.emoji}
        alt={pet.nickname ?? species.name}
        className={styles.image}
        filter={skinFilterFor(pet.active_skin)}
      />
      <div className={styles.renameRow}>
        <input
          className={styles.nameInput}
          value={nameDraft}
          placeholder={species.name}
          onChange={(e) => setNameDraft(e.target.value)}
          maxLength={40}
        />
        <button type="button" className={styles.renameAction} onClick={rename} disabled={busy}>
          Save name
        </button>
      </div>
      <p className={styles.curiosity}>{species.curiosity}</p>
      <div className={styles.stats}>
        {statKeys.map((stat) => (
          <div key={stat} className={styles.statRow}>
            <span className={styles.statLabel}>{stat}</span>
            <span>{Math.round(stats[stat])}</span>
          </div>
        ))}
      </div>
      <div className={styles.actions}>
        {statKeys.map((stat) => (
          <button key={stat} type="button" className={styles.action} onClick={() => care(stat)} disabled={busy}>
            {STAT_LABELS[stat]}
          </button>
        ))}
      </div>
      <SkinPicker
        speciesKey={species.key}
        activeSkin={pet.active_skin}
        ownedSkins={ownedSkins}
        prices={prices}
        who={who}
      />
      {error && (
        <p className={styles.error} role="alert">
          {error}
        </p>
      )}
    </div>
  );
}
```

Nota il cambio di firma: `cost: number | undefined` è sparito dalle prop, sostituito da `prices: Record<string, number>` (da cui `cost` si deriva internamente) e dalla nuova `ownedSkins: string[]` — necessario perché sia il pulsante Unlock sia lo `SkinPicker` leggono prezzi da `item_prices`, ma per chiavi diverse (`species.key` contro le chiavi `skin_*`).

- [ ] **Step 12: Aggiorna `features/pets/PetDetail.test.tsx`**

Il file esistente ha UN SOLO oggetto `pet` (riusato da più test) e QUATTRO chiamate a `render(<PetDetail .../>)`, tutte con `cost={25}` sulla specie `fern` (`key: 'plant_fern'`).

Cambia l'oggetto `pet`:

```ts
const pet: Pet = {
  species_key: 'plant_fern', kind: 'plant', nickname: null,
  stats: { water: 40, light: 100 },
  updated_at: new Date().toISOString(), unlocked_at: new Date().toISOString(),
};
```

in:

```ts
const pet: Pet = {
  species_key: 'plant_fern', kind: 'plant', nickname: null,
  stats: { water: 40, light: 100 },
  updated_at: new Date().toISOString(), unlocked_at: new Date().toISOString(),
  active_skin: null,
};
```

Poi sostituisci, in tutte e 4 le chiamate a `render`, `cost={25}` con
`prices={{ plant_fern: 25 }} ownedSkins={[]}` — cioè ogni riga del tipo

```tsx
    render(<PetDetail species={fern} pet={undefined} cost={25} who="fabrizio" />);
```

o

```tsx
    render(<PetDetail species={fern} pet={pet} cost={25} who="fabrizio" />);
```

diventa rispettivamente

```tsx
    render(<PetDetail species={fern} pet={undefined} prices={{ plant_fern: 25 }} ownedSkins={[]} who="fabrizio" />);
```

```tsx
    render(<PetDetail species={fern} pet={pet} prices={{ plant_fern: 25 }} ownedSkins={[]} who="fabrizio" />);
```

Nessun'altra modifica al file.

Aggiungi un test in fondo al file, prima della chiusura di `describe`:

```tsx
  it('mostra la sezione Skins con l’opzione Natural per un animale sbloccato', () => {
    render(<PetDetail species={fern} pet={pet} prices={{ plant_fern: 25 }} ownedSkins={[]} who="fabrizio" />);
    expect(screen.getByText('Skins')).toBeDefined();
    expect(screen.getByRole('button', { name: 'Natural' })).toBeDefined();
  });
```

- [ ] **Step 13: Esegui e verifica che i test passino**

Run: `npm run test -- features/pets/PetDetail`
Expected: 5 test PASS.

- [ ] **Step 14: Modifica `app/pets/[species]/page.tsx`**

Cambia:

```tsx
  const pet = data?.pets.find((p) => p.species_key === speciesKey);
  const cost = data?.prices[speciesKey];

  return (
    <>
      {offline && <OfflineStrip />}
      {error && (
        <p className={styles.error} role="alert">
          {error}
        </p>
      )}
      <PetDetail species={species} pet={pet} cost={cost} who={who} />
    </>
  );
```

in:

```tsx
  const pet = data?.pets.find((p) => p.species_key === speciesKey);

  return (
    <>
      {offline && <OfflineStrip />}
      {error && (
        <p className={styles.error} role="alert">
          {error}
        </p>
      )}
      <PetDetail species={species} pet={pet} prices={data?.prices ?? {}} ownedSkins={data?.ownedSkins ?? []} who={who} />
    </>
  );
```

- [ ] **Step 15: Aggiorna `app/pets/[species]/page.test.tsx`**

Nel mock `usePets.mockReturnValue({ data: { prices: { pet_dog: 35 }, pets: [] }, ... })`, aggiungi `ownedSkins: []` all'oggetto `data`.

- [ ] **Step 16: Esegui e verifica che il test passi**

Run: `npm run test -- "app/pets/\[species\]/page"`
Expected: 1 test PASS.

- [ ] **Step 17: Esegui l'intera suite unit**

Run: `npm run test`
Expected: tutti i test PASS.

- [ ] **Step 18: Verifica `tsc` e build**

Run: `npx tsc --noEmit && npm run build`
Expected: entrambi puliti.

- [ ] **Step 19: Commit**

```bash
git add features/pets app/pets
git commit -m "feat: filtro skin, SkinPicker e integrazione nelle schermate animali"
```

### Task 5: Doc di deploy in produzione

**Files:**
- Create: `docs/schema-f4.2-skin-animali.sql`

**Interfaces:**
- Consumes: le 3 migrazioni dei Task 1 e 2.
- Produces: file da incollare manualmente nel SQL Editor di Supabase in produzione.

- [ ] **Step 1: Scrivi `docs/schema-f4.2-skin-animali.sql`**

```sql
-- Schema F4.2 di Fabrizio & Emily — skin/colori per animali e piante.
-- Da eseguire SOLO SE F0+F1 (docs/schema-completo.sql) E F4.1
-- (docs/schema-f4.1-animali-piante.sql) sono già stati applicati: questo
-- script presuppone che esistano già item_prices, owned_items,
-- purchase_item, la tabella pets.
-- Incollare per intero nel SQL Editor del progetto Supabase e premere Run,
-- una volta sola.

-- ============================================================
-- 20260824090000_pets_active_skin.sql
-- ============================================================
alter table pets add column active_skin text null;

-- ============================================================
-- 20260824091000_skins_seed_prices.sql
-- ============================================================
insert into item_prices (key, cost, label) values
  ('skin_gold', 50, 'Gold'),
  ('skin_ocean', 50, 'Ocean blue'),
  ('skin_sunset', 50, 'Sunset orange'),
  ('skin_forest', 50, 'Forest green'),
  ('skin_rose', 50, 'Rose pink'),
  ('skin_mint', 50, 'Mint'),
  ('skin_violet', 50, 'Violet'),
  ('skin_charcoal', 50, 'Charcoal');

-- ============================================================
-- 20260824092000_select_pet_skin.sql
-- ============================================================
create or replace function select_pet_skin(
  p_species_key text,
  p_skin_key    text
) returns pets
language plpgsql
security definer
set search_path = public
as $$
declare
  v_pet pets;
begin
  if not exists (select 1 from pets where species_key = p_species_key) then
    raise exception 'pet_not_found';
  end if;

  if p_skin_key is not null and not exists (
    select 1 from owned_items where key = p_skin_key
  ) then
    raise exception 'skin_not_owned';
  end if;

  update pets
     set active_skin = p_skin_key
   where species_key = p_species_key
  returning * into v_pet;

  return v_pet;
end
$$;

revoke all on function select_pet_skin(text, text) from public, anon;
grant execute on function select_pet_skin(text, text) to authenticated;
```

- [ ] **Step 2: Verifica manuale end-to-end** (dopo aver applicato tutti i task su un branch/worktree pulito)

1. `npm run dev`, apri l'app, scegli "Sei Fabrizio", vai su un animale già sbloccato (o sbloccane uno da `/pets`).
2. Apri il dettaglio: in fondo, sotto le azioni di cura, appare la sezione Skins con "Natural" + 8 colori, ciascuno col proprio prezzo.
3. Compra una skin: le monete scalano, il colore si attiva subito e si vede applicato all'emoji/immagine sia nel dettaglio sia tornando alla lista.
4. Attiva "Natural": l'animale torna al colore originale, gratuitamente.
5. Riattiva la skin comprata: nessuna nuova spesa di monete.

Expected: tutti i passaggi funzionano come descritto, nessun errore in console.

- [ ] **Step 3: Verifica `tsc` e build**

Run: `npx tsc --noEmit && npm run build`
Expected: entrambi puliti.

- [ ] **Step 4: Commit**

```bash
git add docs/schema-f4.2-skin-animali.sql
git commit -m "docs: schema di deploy in produzione per F4.2"
```
